name: Deploy

on:
  push:
    branches:
      - master

env:
  URL: 'https://pterodactyl-eggs.faithnode.com/'
  AUTHOR: 'admin@faithnode.com'
  COMMENT: 'GENERATED WITH FAITHNODE'

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build toolbox image
        run: docker build -t pterodactyl-eggs-toolbox -f Dockerfile .

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build
        run: |
          docker run --rm -v "$PWD:/repo" -w /repo \
            -e URL -e AUTHOR -e COMMENT \
            pterodactyl-eggs-toolbox bin/build.sh

      - name: Generate Index
        run: |
          docker run --rm -v "$PWD:/repo" -w /repo \
            -e URL \
            pterodactyl-eggs-toolbox bin/index.sh

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './.dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
